require 'spec_helper'

describe Wix::Hive::Activities do

  context 'factory methods' do
    # it '#CONTACT_FORM' do
    #   expect(Wix::Hive::Activities::CONTACT_FORM.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::CONTACT_FORM.type)
    # end
    #
    # it '#CREATE' do
    #   expect(Wix::Hive::Activities::CREATE.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::CREATE.type)
    # end
    #
    # it '#COMPLETE' do
    #   expect(Wix::Hive::Activities::COMPLETE.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::COMPLETE.type)
    # end
    #
    # it '#PURCHASE' do
    #   expect(Wix::Hive::Activities::PURCHASE.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::PURCHASE.type)
    # end
    #
    # it '#SEND' do
    #   expect(Wix::Hive::Activities::SEND.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::SEND.type)
    # end
    #
    # it '#ALBUM_FAN' do
    #   expect(Wix::Hive::Activities::ALBUM_FAN.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::ALBUM_FAN.type)
    # end
    #
    # it '#ALBUM_SHARE' do
    #   expect(Wix::Hive::Activities::ALBUM_SHARE.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::ALBUM_SHARE.type)
    # end
    #
    # it '#TRACK_LYRICS' do
    #   expect(Wix::Hive::Activities::TRACK_LYRICS.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::TRACK_LYRICS.type)
    # end
    #
    # it '#TRACK_PLAY' do
    #   expect(Wix::Hive::Activities::TRACK_PLAY.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::TRACK_PLAY.type)
    # end
    #
    # it '#TRACK_PLAYED' do
    #   expect(Wix::Hive::Activities::TRACK_PLAYED.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::TRACK_PLAYED.type)
    # end
    #
    # it '#TRACK_SKIP' do
    #   expect(Wix::Hive::Activities::TRACK_SKIP.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::TRACK_SKIP.type)
    # end
    #
    # it '#TRACK_SHARE' do
    #   expect(Wix::Hive::Activities::TRACK_SHARE.klass).to eq Wix::Hive::Activities.class_for_type(Wix::Hive::Activities::TRACK_SHARE.type)
    # end
    #
    # it 'Invalid type' do
    #   expect(Wix::Hive::Activities.class_for_type('invalid')).to be_nil
    # end
  end
end